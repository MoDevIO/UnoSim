{
  "version": 2,
  "buildCommand": "npx vite build client/ --config vite.config.ts --base ./ && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/server.js",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.js"
    },
    {
      "source": "/ws",
      "destination": "/api/server.js"
    }
  ]
}