# Test environment overrides
# Loaded by Vitest automatically (mode=test) — overrides .env values.
# Ensures React loads its development build and act() is available.
NODE_ENV=test
ENABLE_TEST_ENDPOINTS=true
