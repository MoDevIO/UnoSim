version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: unowebsim:latest
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    # volumes for local development (uncomment when developing locally)
    # volumes:
    #   - ./:/app
    #   - /app/node_modules
